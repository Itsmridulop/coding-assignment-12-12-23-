#include<stdio.h>
int main()
{
	int a,remainder,rev=0,num;
	printf("Enter a number");
	scanf("%d",&num);
	int temp=num;
	while(num!=0)
	{
		remiander=num%10;
		rev=rev*10+remainder;
		num/=10;
	}
	if(temp==rev)
	  printf("It is palindrone");
	else
	  printf("It is not palindrone");
	return 0;
}
